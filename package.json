{
  "name": "vite-starter",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "start": "cross-env PORT=$PORT serve dist -l $PORT"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "serve": "^14.2.4",
    "vite": "^5.4.2"
  },
  "dependencies": {
    "inputmask": "^5.0.9"
  }
}
